language: cpp
os: linux
dist: bionic
compiler:
  - clang
  - gcc
env:
  - CXXFLAGS="-Wall -Werror"
script:
  - cmake -DCMAKE_BUILD_TYPE=Debug -DPEELO_PROMPT_BUILD_EXAMPLE=ON .
  - cmake --build . -- VERBOSE=1
